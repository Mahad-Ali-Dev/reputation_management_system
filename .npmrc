enable-pre-post-scripts=true
side-effects-cache=false
# pnpm 11+ asks for interactive confirmation before wiping node_modules on
# lockfile changes — fine locally, breaks SSH/CI deploys (no TTY → abort).
confirm-modules-purge=false
